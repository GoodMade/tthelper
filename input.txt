[System Instructions: Ты AI помощник-разработчик для конструктора TapTop. Твоя задача — переводить обычные HTML/CSS/JS идеи в структуру слоев конструктора.
Отвечай только валидным JSON без markdown.
Ответ всегда должен начинаться символом "{" и заканчиваться символом "}". Не возвращай JSON-фрагменты, массивы верхнего уровня, raw CSS или пояснения вне JSON.
JSON должен быть компактным: без форматирования, без комментариев, без повторения исходного CSS текстом. Не обрывай ответ; если компонент слишком большой, упрости структуру, но верни закрытый валидный JSON.
Формат ответа: {"message":"коротко что создано","constructor":{"name":"component-name","root":{...},"embedCode":"<style>...</style><script>...</script>"}}.
constructor.root — дерево слоев. Каждый слой: {"type":"div|text|link|image|svg|embed","name":"Layer name","classes":["bem-class"],"attrs":{"data-widget":"name"},"styles":{"display":"flex"},"mediaStyles":{"(max-width: 479px)":{"display":"block"}},"children":[...]}.
Никогда не отвечай только CSS вроде display:grid или raw HTML. Даже для короткой задачи возвращай constructor.root.
Для структурных задач создавай реальные дочерние слои, а не только стили контейнера.
Правила перевода: div/section/article -> type "div" (Div Block); текстовые теги h1-h6/p/span -> type "text" и поле text/html; a/button -> type "link" с href и дочерним text; img -> type "image"; svg -> type "svg"; script -> отдельный embedCode.
Для SVG-иконок всегда возвращай type "svg" и поле svg или html с полным inline <svg>...</svg>. Если исходник выглядит как <span class="svg-icon"><svg>...</svg></span>, это один слой type "svg", а не text/span.
Обычные inline style="" и root-селекторы переноси в styles/mediaStyles конкретных слоев.
CSS-правила из исходного <style> не возвращай в JSON как classStyles/mediaClassStyles: расширение само распарсит исходный <style> и перенесет эти стили в классы TapTop.
Сохраняй классы из HTML на слоях. Составные CSS-селекторы из исходного <style> расширение расплющит само.
Не используй shorthand gap в ответе. gap:20px возвращай как row-gap:"20px" и column-gap:"20px"; gap:10px 20px — row-gap:"10px", column-gap:"20px".
В embedCode клади внешние <link rel="stylesheet">, внешние <script src>, в��сь JS и только CSS, который нельзя выразить стилями TapTop: keyframes, CSS-переменные, псевдоклассы/hover, псевдоэлементы, сложные глобальные селекторы, библиотечные служебные классы и JS-зависимые состояния.
Классы указывай без точки, в BEM/читабельном виде. JS должен искать элементы по data-* атрибутам или классам, которые есть в слоях.
Если нужен JS, embedCode должен содержать весь нужный <script>; если нужны внешние библиотеки, сохрани подключения в embedCode. Embed будет создан отдельным скрытым слоем внутри компонента.
Если в prompt есть selected_layer/selected_layout, считай его точкой вставки на холсте: constructor будет добавлен внутрь выбранного слоя. Не возвращай replaceSelectedLayer в builder-mode, если пользователь явно не просит заменить выбранный слой.
Не возвращай raw HTML как финальный результат. Всегда возвращай constructor-spec, который можно собрать в слои TapTop.]

Задача пользователя:
Сверстай по коду
Переведи исходную верстку из вкладки "Верстка" в constructor.root TapTop.
HTML-структуру из вкладки "Верстка" разложи на слои TapTop.
Inline style="" пе��енеси в styles/mediaStyles соответствующих слоев.
CSS из вкладки "Стили" пользователь присылает без тега <style>. Считай его исходным CSS компонента.
Сохрани классы из HTML на соответствующих слоях, но не возвращай большой classStyles/mediaClassStyles из исходного <style>: сборщик расширения сам перенесет CSS классов из <style> в TapTop-классы.
CSS из вкладки "Стили" и @media в нем тоже обработает сборщик расширения. Если ты добавляешь новые адаптивные стили сверх исходника, используй mediaStyles с breakpoint TapTop: 991px, 767px, 479px.
Составные class-селекторы из исходного CSS сборщик расплющит сам. Если создаешь такие классы вручную, используй BEM-класс на целевом слое: .card-green .stat-number -> card-green__stat-number.
SVG-иконки переноси как слой type "svg" с полным inline <svg>...</svg> в поле svg/html. <span class="svg-icon"><svg>...</svg></span> считается одним SVG-слоем.
gap не возвращай как gap; если задаешь gap вручную, используй row-gap и column-gap.
Не переноси CSS простых классов в root/уникальный класс слоя. Исключение — только классы из правила "Классы > корневой класс слоя".
Код из вкладки "Скрипт" не конвертируй в слои. Помести его целиком в constructor.embedCode.
В constructor.embedCode также оставь внешние <link rel="stylesheet">, внешние <script src>, весь JS и только CSS из вкладки "Стили", который нельзя задать как TapTop-стили.
Сохрани классы/data-атрибуты, нужные для JS, Swiper и навигации.
Правила конвертации из вкладки "Правила". Эти правила имеют приоритет над общими правилами конструктора.

Классы, которые нужно сохранить:
swiper
swiper-wrapper
swiper-slide
swiper-button-disabled
custom-prev
custom-next
Оставь эти классы на соответствующих слоях, особенно если их ищет JS или библиотека.

CSS, который допустим в embedCode:
:hover
:focus
:active
::before
::after
@keyframes
.swiper-button-disabled
Эти сложные CSS-состояния/конструкции можно оставлять в <style> embedCode. Обычные свойства не дублируй между styles/mediaStyles и embedCode.

Защитные запреты:
max-width: 120px unless-source
height: 0px unless-source
min-height: 0px unless-source
Не придумывай эти стили, если их нет в исходнике или задаче пользователя. Особенно не добавляй случайные max-width/height со служебными значениями.

Обычные стили TapTop:
width
height
min-width
max-width
min-height
max-height
display
position
top
right
bottom
left
margin
margin-top
margin-right
margin-bottom
margin-left
padding
padding-top
padding-right
padding-bottom
padding-left
row-gap
column-gap
flex-direction
justify-content
align-items
flex-wrap
grid-template-columns
border
border-radius
background
color
font-size
font-weight
line-height
letter-spacing
opacity
overflow
Для этих свойств используй styles/mediaStyles, если стиль пришел из inline/root-селектора или создан вручную. Не используй custom properties для стандартных значений; custom допускается только для calc/var/неподдерживаемых значений.
<TT_LAYOUT_CODE>
<div class="cards" id="cards">
    <!-- Card 1 -->
    <article class="card card-1">
      <div class="card-head">
        <h3 class="card-title">Find your flow</h3>
        <span class="card-sub">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round"><path d="M4 12h.01M9 12h.01M14 12h.01M19 12h.01"/></svg>
          Planning your lesson
        </span>
      </div>
      <div class="card-foot">
        <button class="listen-btn" data-type="video" data-title="Find your flow" data-subtitle="Planning your lesson" data-src="https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/BigBuckBunny.mp4">
          <span class="eq-icon"></span> Слушать
        </button>
        <span class="card-caption">Average time saved per lesson</span>
      </div>
    </article>

    <!-- Card 2 -->
    <article class="card card-2">
      <div class="card-head">
        <h3 class="card-title">Focus your mind</h3>
        <span class="card-sub">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round"><path d="M4 9v6M8 5v14M12 8v8M16 4v16M20 9v6"/></svg>
          Capturing the discussion
        </span>
      </div>
      <div class="card-foot">
        <button class="listen-btn" data-type="audio" data-title="Focus your mind" data-subtitle="Capturing the discussion" data-src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3">
          <span class="eq-icon"></span> Слушать
        </button>
        <span class="card-caption">Beta users that liked or loved Bobbin</span>
      </div>
    </article>

    <!-- Card 3 -->
    <article class="card card-3">
      <div class="card-head">
        <h3 class="card-title">Feel your value</h3>
        <span class="card-sub">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M4 12.5l5 5 11-11"/></svg>
          Sending progress report
        </span>
      </div>
      <div class="card-foot">
        <button class="listen-btn" data-type="audio" data-title="Feel your value" data-subtitle="Sending progress report" data-src="https://www.soundhelix.com/examples/mp3/SoundHelix-Song-2.mp3">
          <span class="eq-icon"></span> Слушать
        </button>
        <span class="card-caption">Lesson report satisfaction rate</span>
      </div>
    </article>
  </div>

  <div class="dots" id="dots"></div>

  <!-- Popup player -->
  <div class="player-overlay" id="overlay" aria-hidden="true">
    <div class="player" role="dialog" aria-modal="true" aria-label="Аудио плеер">
      <div class="player-top">
        <div class="player-badge"><span class="eq-icon"></span></div>
        <div class="player-meta">
          <strong id="pTitle">Title</strong>
          <span id="pSubtitle">Subtitle</span>
        </div>
        <button class="player-close" id="pClose" aria-label="Закрыть">&times;</button>
      </div>
      <div class="player-body" id="pBody"></div>
    </div>
  </div>

  <!-- Reusable animated equalizer icon template -->
  <template id="eqTemplate">
    <svg class="eq" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 250 250" preserveAspectRatio="xMidYMid meet" aria-hidden="true">
      <g>
        <g class="eq-bar" transform="matrix(1,0,0,1,5,50.08747863769531)"><path fill="rgb(29,24,19)" d="M40,20 C40,8.961999893188477 31.038000106811523,0 20,0 C20,0 20,0 20,0 C8.961999893188477,0 0,8.961999893188477 0,20 C0,20 0,129.82504272460938 0,129.82504272460938 C0,140.863037109375 8.961999893188477,149.82504272460938 20,149.82504272460938 C20,149.82504272460938 20,149.82504272460938 20,149.82504272460938 C31.038000106811523,149.82504272460938 40,140.863037109375 40,129.82504272460938 C40,129.82504272460938 40,20 40,20z"/></g>
        <g class="eq-bar" transform="matrix(1,0,0,1,56,74.80192565917969)"><path fill="rgb(29,24,19)" d="M40,20 C40,8.961999893188477 31.038000106811523,0 20,0 C20,0 20,0 20,0 C8.961999893188477,0 0,8.961999893188477 0,20 C0,20 0,80.3961410522461 0,80.3961410522461 C0,91.43414306640625 8.961999893188477,100.3961410522461 20,100.3961410522461 C20,100.3961410522461 20,100.3961410522461 20,100.3961410522461 C31.038000106811523,100.3961410522461 40,91.43414306640625 40,80.3961410522461 C40,80.3961410522461 40,20 40,20z"/></g>
        <g class="eq-bar" transform="matrix(1,0,0,1,105,60.685340881347656)"><path fill="rgb(29,24,19)" d="M40,20 C40,8.961999893188477 31.038000106811523,0 20,0 C20,0 20,0 20,0 C8.961999893188477,0 0,8.961999893188477 0,20 C0,20 0,108.62931823730469 0,108.62931823730469 C0,119.66732025146484 8.961999893188477,128.6293182373047 20,128.6293182373047 C20,128.6293182373047 20,128.6293182373047 20,128.6293182373047 C31.038000106811523,128.6293182373047 40,119.66732025146484 40,108.62931823730469 C40,108.62931823730469 40,20 40,20z"/></g>
        <g class="eq-bar" transform="matrix(1,0,0,1,155,36.781883239746094)"><path fill="rgb(29,24,19)" d="M40,20 C40,8.961999893188477 31.038000106811523,0 20,0 C20,0 20,0 20,0 C8.961999893188477,0 0,8.961999893188477 0,20 C0,20 0,156.4362335205078 0,156.4362335205078 C0,167.47422790527344 8.961999893188477,176.4362335205078 20,176.4362335205078 C20,176.4362335205078 20,176.4362335205078 20,176.4362335205078 C31.038000106811523,176.4362335205078 40,167.47422790527344 40,156.4362335205078 C40,156.4362335205078 40,20 40,20z"/></g>
        <g class="eq-bar" transform="matrix(1,0,0,1,205,69.3953857421875)"><path fill="rgb(29,24,19)" d="M40,20 C40,8.961999893188477 31.038000106811523,0 20,0 C20,0 20,0 20,0 C8.961999893188477,0 0,8.961999893188477 0,20 C0,20 0,91.209228515625 0,91.209228515625 C0,102.24723052978516 8.961999893188477,111.209228515625 20,111.209228515625 C20,111.209228515625 20,111.209228515625 20,111.209228515625 C31.038000106811523,111.209228515625 40,102.24723052978516 40,91.209228515625 C40,91.209228515625 40,20 40,20z"/></g>
      </g>
    </svg>
  </template>
</TT_LAYOUT_CODE>
<TT_STYLE_CODE>
body {
    background: var(--bg);
    color: var(--ink);
    font-family: "Inter", system-ui, -apple-system, "Segoe UI", sans-serif;
    -webkit-font-smoothing: antialiased;
    padding: 48px 24px;
  }

  /* ---------- Layout ---------- */
  .cards {
    max-width: 1080px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 20px;
  }

  .card {
    position: relative;
    aspect-ratio: 300 / 380;
    border-radius: var(--radius);
    overflow: hidden;
    background-size: cover;
    background-position: center;
    background-color: #cdbfa9;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    isolation: isolate;
  }

  /* darkening gradients top + bottom for legibility */
  .card::before {
    content: "";
    position: absolute;
    inset: 0;
    z-index: -1;
    background:
      linear-gradient(to bottom, rgba(20,15,10,.55) 0%, rgba(20,15,10,0) 32%),
      linear-gradient(to top, rgba(20,15,10,.65) 0%, rgba(20,15,10,0) 42%);
  }

  .card-1 { background-image: url("https://images.unsplash.com/photo-1573497019940-1c28c88b4f3e?q=80&w=900&auto=format&fit=crop"); }
  .card-2 { background-image: url("https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?q=80&w=900&auto=format&fit=crop"); }
  .card-3 { background-image: url("https://images.unsplash.com/photo-1521737604893-d14cc237f11d?q=80&w=900&auto=format&fit=crop"); }

  .card-head {
    padding: 26px 26px 0;
    text-align: center;
    color: #fff;
  }

  .card-title {
    font-family: "Lora", Georgia, "Times New Roman", serif;
    font-weight: 600;
    font-size: clamp(24px, 2.2vw, 30px);
    line-height: 1.1;
    margin: 0 0 12px;
    letter-spacing: .2px;
  }

  .card-sub {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    font-size: 14px;
    font-weight: 500;
    opacity: .96;
  }
  .card-sub svg { width: 16px; height: 16px; flex: none; }

  .card-foot {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    padding: 22px;
  }

  .card-caption {
    color: #fff;
    font-size: 13.5px;
    line-height: 1.3;
    text-align: right;
    max-width: 52%;
  }

  /* ---------- Listen button ---------- */
  .listen-btn {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    border: 0;
    cursor: pointer;
    background: var(--yellow);
    color: var(--ink);
    font-family: inherit;
    font-size: 14.5px;
    font-weight: 600;
    padding: 11px 18px 11px 14px;
    border-radius: 999px;
    line-height: 1;
    transition: transform .15s ease, background .15s ease, box-shadow .15s ease;
    box-shadow: 0 6px 18px rgba(0,0,0,.18);
  }
  .listen-btn:hover { transform: translateY(-1px); box-shadow: 0 10px 22px rgba(0,0,0,.22); }
  .listen-btn:active { transform: translateY(0); background: var(--yellow-press); }
  .listen-btn:focus-visible { outline: 3px solid rgba(29,24,19,.35); outline-offset: 2px; }

  /* animated equalizer icon */
  .eq { width: 18px; height: 18px; display: block; }
  .eq-bar {
    transform-box: fill-box;
    transform-origin: center;
    animation: eq 1.1s ease-in-out infinite;
  }
  .eq-bar:nth-child(1) { animation-delay: -.9s; }
  .eq-bar:nth-child(2) { animation-delay: -.5s; }
  .eq-bar:nth-child(3) { animation-delay: -.2s; }
  .eq-bar:nth-child(4) { animation-delay: -.7s; }
  .eq-bar:nth-child(5) { animation-delay: -.35s; }
  /* play faster while audio/video is playing */
  .listen-btn.is-playing .eq-bar { animation-duration: .55s; }

  @keyframes eq {
    0%, 100% { transform: scaleY(.45); }
    50%      { transform: scaleY(1); }
  }
  @media (prefers-reduced-motion: reduce) {
    .eq-bar { animation: none; transform: scaleY(.85); }
  }

  /* ---------- Mobile slider ---------- */
  @media (max-width: 900px) {
    body { padding: 32px 0; }
    .cards {
      display: flex;
      gap: 16px;
      overflow-x: auto;
      scroll-snap-type: x mandatory;
      -webkit-overflow-scrolling: touch;
      padding: 0 20px 14px;
      scrollbar-width: none;
    }
    .cards::-webkit-scrollbar { display: none; }
    .card {
      flex: 0 0 70%;
      max-width: 380px;
      scroll-snap-align: center;
      aspect-ratio: 300 / 400;
    }
  }

  /* phones: show a bit more of the next card */
  @media (max-width: 560px) {
    .card { flex: 0 0 82%; }
  }

  /* slider dots */
  .dots {
    display: none;
    justify-content: center;
    gap: 8px;
    margin-top: 16px;
  }
  .dots button {
    width: 8px; height: 8px; padding: 0;
    border: 0; border-radius: 999px;
    background: rgba(29,24,19,.22);
    cursor: pointer;
    transition: width .2s ease, background .2s ease;
  }
  .dots button.active { width: 22px; background: var(--ink); }
  @media (max-width: 900px) { .dots { display: flex; } }

  /* ---------- Popup player ---------- */
  .player-overlay {
    position: fixed;
    inset: 0;
    z-index: 50;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 20px;
    background: rgba(20,15,10,.55);
    backdrop-filter: blur(4px);
    opacity: 0;
    pointer-events: none;
    transition: opacity .2s ease;
  }
  .player-overlay.open { opacity: 1; pointer-events: auto; }

  .player {
    width: min(420px, 100%);
    background: #fff;
    border-radius: 22px;
    overflow: hidden;
    box-shadow: 0 30px 80px rgba(0,0,0,.4);
    transform: translateY(12px) scale(.98);
    transition: transform .22s ease;
  }
  .player-overlay.open .player { transform: translateY(0) scale(1); }

  .player-top {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 16px 16px 14px;
    border-bottom: 1px solid #efece4;
  }
  .player-badge {
    width: 42px; height: 42px; flex: none;
    border-radius: 12px;
    background: var(--yellow);
    display: flex; align-items: center; justify-content: center;
  }
  .player-badge .eq { width: 22px; height: 22px; }
  .player-meta { flex: 1; min-width: 0; }
  .player-meta strong { display: block; font-size: 15px; }
  .player-meta span { font-size: 12.5px; color: #8a8377; }
  .player-close {
    border: 0; background: #f1eee7; color: var(--ink);
    width: 34px; height: 34px; border-radius: 50%;
    font-size: 18px; cursor: pointer; line-height: 1;
    transition: background .15s ease;
  }
  .player-close:hover { background: #e6e2d8; }

  .player-body { padding: 16px; }
  .player-body video {
    width: 100%;
    border-radius: 12px;
    display: block;
    background: #000;
  }
  .player-body audio { width: 100%; display: block; }
  .player-body.audio-mode {
    padding: 22px 18px 24px;
  }
</TT_STYLE_CODE>
<TT_EMBED_CODE>
<script>
    // Inject the animated equalizer icon everywhere it's needed
    const eqTpl = document.getElementById("eqTemplate").content;
    document.querySelectorAll(".eq-icon").forEach(slot => {
      slot.appendChild(eqTpl.cloneNode(true));
    });

    /* ---------- Popup player logic ---------- */
    const overlay = document.getElementById("overlay");
    const pBody   = document.getElementById("pBody");
    const pTitle  = document.getElementById("pTitle");
    const pSub    = document.getElementById("pSubtitle");
    const pClose  = document.getElementById("pClose");
    let activeBtn = null;

    function openPlayer(btn) {
      const { type, src, title, subtitle } = btn.dataset;
      pTitle.textContent = title || "";
      pSub.textContent = subtitle || "";
      pBody.innerHTML = "";
      pBody.classList.toggle("audio-mode", type === "audio");

      const media = document.createElement(type === "video" ? "video" : "audio");
      media.src = src;
      media.controls = true;
      media.autoplay = true;
      media.playsInline = true;
      pBody.appendChild(media);

      // reflect playback state on the card button icon
      activeBtn = btn;
      media.addEventListener("play",  () => btn.classList.add("is-playing"));
      media.addEventListener("pause", () => btn.classList.remove("is-playing"));
      media.addEventListener("ended", () => btn.classList.remove("is-playing"));

      overlay.classList.add("open");
      overlay.setAttribute("aria-hidden", "false");
      // attempt autoplay (browsers may need muted for video autoplay)
      const tryPlay = media.play();
      if (tryPlay && tryPlay.catch) {
        tryPlay.catch(() => { if (type === "video") { media.muted = true; media.play().catch(()=>{}); } });
      }
    }

    function closePlayer() {
      const media = pBody.querySelector("video, audio");
      if (media) { media.pause(); media.removeAttribute("src"); media.load && media.load(); }
      pBody.innerHTML = "";
      overlay.classList.remove("open");
      overlay.setAttribute("aria-hidden", "true");
      if (activeBtn) { activeBtn.classList.remove("is-playing"); activeBtn = null; }
    }

    document.querySelectorAll(".listen-btn").forEach(btn => {
      btn.addEventListener("click", () => openPlayer(btn));
    });
    pClose.addEventListener("click", closePlayer);
    overlay.addEventListener("click", e => { if (e.target === overlay) closePlayer(); });
    document.addEventListener("keydown", e => { if (e.key === "Escape" && overlay.classList.contains("open")) closePlayer(); });

    /* ---------- Mobile slider dots ---------- */
    const cards = document.getElementById("cards");
    const dotsWrap = document.getElementById("dots");
    const slides = Array.from(cards.children);

    slides.forEach((_, i) => {
      const d = document.createElement("button");
      d.setAttribute("aria-label", "Слайд " + (i + 1));
      d.addEventListener("click", () => {
        slides[i].scrollIntoView({ behavior: "smooth", inline: "center", block: "nearest" });
      });
      dotsWrap.appendChild(d);
    });

    function updateDots() {
      const center = cards.scrollLeft + cards.clientWidth / 2;
      let idx = 0, best = Infinity;
      slides.forEach((s, i) => {
        const sc = s.offsetLeft + s.offsetWidth / 2;
        const dist = Math.abs(sc - center);
        if (dist < best) { best = dist; idx = i; }
      });
      dotsWrap.querySelectorAll("button").forEach((b, i) => b.classList.toggle("active", i === idx));
    }
    cards.addEventListener("scroll", () => window.requestAnimationFrame(updateDots), { passive: true });
    window.addEventListener("resize", updateDots);
    updateDots();
  </script>
</TT_EMBED_CODE>


Опциональная точка вставки/контекст выбранного слоя JSON:
{
  "selected_layer": null,
  "selected_layout": null
}

Верни цельный JSON с constructor.root. Не возвращай CSS-only, raw HTML или текстовое описание вместо слоя. Если selected_layer не null, компонент бу��ет вставлен вн��т��ь этого слоя на холсте. Если пользователь прислал HTML/CSS/JS, переведи HTML в слои TapTop, inline/root CSS — в styles/mediaStyles, классы сохрани на слоях, JS и внешние подключения — в embedCode. CSS ��ростых классов из исходного <style> не повторяй в JSON: его обработает локальный сборщик.